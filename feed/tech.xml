---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site.title }} — 技術</title>
  <link href="{{ site.url }}/feed/tech.xml" rel="self" type="application/atom+xml"/>
  <link href="{{ site.url }}/" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/feed/tech.xml</id>
  <author>
    <name>{{ site.author.name }}</name>
  </author>
  {% for post in site.posts limit:20 %}
  {% unless post.categories contains 'macro' %}
  <entry>
    <title type="html">{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    <id>{{ post.url | absolute_url }}</id>
    <content type="html">{{ post.content | xml_escape | truncatewords: 200 }}</content>
    {% if post.description %}
    <summary>{{ post.description | xml_escape }}</summary>
    {% endif %}
    {% for tag in post.tags %}
    <category term="{{ tag }}"/>
    {% endfor %}
  </entry>
  {% endunless %}
  {% endfor %}
</feed>
